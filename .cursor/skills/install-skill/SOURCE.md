# install-skill as it existed in the apps

Copied from `Engine-side-/.cursor/skills/install-skill/SKILL.md`
(same text as `strengthside/vendor/skills/install-skill/SKILL.md`).

No public GitHub repository publishes this file. Unique phrases from it
(`skill-vet`, `.$NAME-INSTALLED`, "placing skill directories where they are
read") do not match any indexed public repo. strengthside `skills.md` records
it as **pre-existing in the hybrid tree**, not as an `npx skills add` pin.

The public GitHub for installing skills from GitHub is:

- https://github.com/vercel-labs/skills (`npx skills add …`) — vendored at
  `vendor/github/vercel-labs/skills`
- https://github.com/mikekelly/managing-skills — vendored at
  `vendor/github/mikekelly/managing-skills`
